$(document).ready(function () {    $("#f-content").validate({        rules: {            name: {                required: true            }        },        messages: {            name: {                required: 'Nhập tên phường/xã'            }        },        submitHandler: function (form) {            form.submit();        },        highlight: function (element) {            $(element).closest('.form-group').addClass('has-error');        },        unhighlight: function (element) {            $(element).closest('.form-group').removeClass('has-error');        },        errorElement: 'span',        errorClass: 'help-block',        errorPlacement: function (error, element) {            if (element.parent('.input-group').length) {                error.insertAfter(element.parent());            } else {                error.insertAfter(element);            }        }    });});$(document).on('change', "#pId", function (e) {	var province = parseInt($(this).val());	if(province == 0){		$("#district_id").html('<option value="0">-- Vui lòng chọn quận/huyện --</option>');		return false;	}	var data = {		'id': province,		'not_display_first': 1	};	$.ajax({		url: base_url + 'gets-district-ajax',		data: data,		type: 'POST',		dataType: 'json',		success: function (response) {			if (response.status === 'success') {				$("#district_id").html(response.content.district);			}		},		error: function (e) {			console.log('Error processing your request: ' + e.responseText);		}	});	return false;});